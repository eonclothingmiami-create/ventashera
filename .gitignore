# OS / editor
.DS_Store
Thumbs.db
*.tmp

# Local backups (kept during migration)
index.backup.inline.html
src_temp_full.js

# Optional: remove from ignore if you want to version this backup
# src/js/full.inline.backup.js
